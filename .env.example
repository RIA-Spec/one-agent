# OpenTelemetry / SigNoz Configuration
# Service name that will appear in SigNoz traces
OTEL_SERVICE_NAME=one-agent

# SigNoz OTLP endpoint (default: http://localhost:4318/v1/traces)
# Change this if your SigNoz is running on a different host/port
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces

# AI SDK / Model API Keys
# Add your API keys here
# ANTHROPIC_API_KEY=your_key_here
# OPENAI_API_KEY=your_key_here
# EXA_API_KEY=your_key_here
# ONE_EXA_API_KEY=your_key_here

# Reason-able Action Space (RAS) Configuration
# Controls which runtime surface to enable: "python" | "bash"
# Only one mode can be active at a time
# - python: Enable Python RAS (Code Interpreter approach) - default
# - bash: Enable Bash RAS (Unix philosophy approach)
RAS_MODE=python

# File System Configuration for RAS
# Root directory for file operations (defaults to project root)
# NODE_FS_ROOT=/path/to/root
# Mount point path visible in Python/Bash (defaults to project root)
# NODE_FS_MOUNT_POINT=/path/to/mount


# AI SDK / Model API Keys
# Add your API keys here
# ANTHROPIC_API_KEY=your_key_here
OPENAI_BASE_URL=
OPENAI_API_KEY=

# Chat model configuration
# Backend uses ONE_CHAT_MODEL to choose the runtime model.
# Frontend uses NEXT_PUBLIC_CHAT_MODEL only for initial UI metadata.
ONE_CHAT_MODEL=gemini-3.1-pro
NEXT_PUBLIC_CHAT_MODEL=gemini-3.1-pro
