# Python Agent

An AI agent with Python code execution capabilities using MCP.

## Usage

```typescript
import { agent } from "./src/agent";

await agent("Calculate fibonacci sequence up to 100");
```

## Development

```bash
pnpm install
pnpm run build
pnpm run typecheck
```
